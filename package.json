{
  "name": "calculators_tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test-jenkins": "JUNIT_REPORT_PATH=/report.xml mocha --colors --reporter mocha-jenkins-reporter",
    "test": "mocha"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "allure-mocha": "^2.0.0-beta.8"
  }
}
